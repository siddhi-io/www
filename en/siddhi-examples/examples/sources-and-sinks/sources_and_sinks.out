#### Input

When a JSON message in the following default message format is sent to url `http://0.0.0.0:8006/temp` with content type `application/json`.

```json
{
   "event":{
      "sensorId":"aq-14",
      "temperature":35.4
   }
}
```

It will be automatically mapped to the `TemperatureStream` stream.

To process custom input messages refer the examples related to Input Data Mapping.

#### Output

After processing, the event will be logged to the console via the `log` sink of `TemperatureOnlyStream` stream as following;

```bash
Event{timestamp=1574515771712, data=[35.4], isExpired=false}
```

Here the `log` sink by default uses `passThrough` mapper, and hence directly logs the Siddhi Events.

To output messages on custom message format, pleases refer the examples related to Output Data Mapping.