Provides introduction to [sources](../../query-guide#source) and [sink](../../query-guide#sink) that are used to consume and publish events to external systems.

There are multiple source and sink types, but this example only explains `http` source, `log` sink, and `kafka` sink. For more info refer the Siddhi [query guide](../../query-guide).